# Direct dependencies only, pinned to the exact versions the test suite runs
# against — pinning avoids dependency-resolution work on every Streamlit Cloud
# rebuild and guarantees the deployed env matches the tested one.
# NOTE: scikit-learn was removed (nothing imports it); statsmodels stays — it
# powers the AMCIP forecast models (SES/Holt/ETS/ARIMA/SARIMA/ADF test).
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
plotly==6.9.0
statsmodels==0.14.6
streamlit==1.59.2
openpyxl==3.1.5
pydantic==2.13.4
PyYAML==6.0.1
pyarrow==24.0.0
pytest==9.1.1
